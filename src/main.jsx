import React from "react";
import ReactDOM from "react-dom/client";
import AuraLinkApp from "./AuraLinkApp";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AuraLinkApp />
  </React.StrictMode>
);